# Copy this file to ".env" and fill in your real MySQL credentials.
# The .env file itself is git-ignored so your password never gets committed.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=computerproject
